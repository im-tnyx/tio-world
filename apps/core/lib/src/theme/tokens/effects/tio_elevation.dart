/// Reusable Material/effect elevation roles.
///
/// Elevation is an effect contract rather than layout geometry, even when its
/// physical value is numeric. Add roles only when repeated component/theme
/// evidence justifies shared ownership.
abstract final class TioElevation {
  static const none = 0.0;
}
