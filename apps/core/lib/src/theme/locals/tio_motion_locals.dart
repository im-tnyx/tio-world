import '../tokens/effects/tio_motion.dart';

class TioMotionLocals {
  const TioMotionLocals._();

  static const fastMs = TioMotion.fastMs;
  static const normalMs = TioMotion.normalMs;
  static const slowMs = TioMotion.slowMs;
}
