import 'package:flutter/material.dart';

class TioTheme extends StatelessWidget {
  const TioTheme({super.key, required this.child});

  final Widget child;

  @override
  Widget build(BuildContext context) {
    return child;
  }
}
