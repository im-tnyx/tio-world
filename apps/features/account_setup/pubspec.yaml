name: tio_feature_account_setup
publish_to: none

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  tio_core:
    path: ../../core
  tio_feature_profile:
    path: ../profile
  tio_shared:
    path: ../../shared

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
