name: tio_feature_settings
publish_to: none

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  shared_preferences: ^2.5.5
  go_router: ^14.6.2
  tio_core:
    path: ../../core
  tio_shared:
    path: ../../shared
  tio_feature_progress:
    path: ../progress
  flutter_svg: ^2.3.0
  image_picker: ^1.1.2

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  shared_preferences_platform_interface: ^2.4.2
