name: Attribution App pin probe

on:
  pull_request:
    branches:
      - main

permissions: {}

jobs:
  spoof:
    name: Commit attribution guard
    runs-on: ubuntu-latest
    steps:
      - name: Fail deliberately
        run: exit 1
